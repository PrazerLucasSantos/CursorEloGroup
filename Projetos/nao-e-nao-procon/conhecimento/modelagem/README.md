# Modelagem

Inventário gerado a partir do épico em `../../data/epics/`.

Para regenerar:

```powershell
cd ..\..\  # raiz do projeto se estiver em scripts; ou rode:
node scripts/_gen-nen-modelagem-md.mjs
```

(ajuste o script para gravar em `conhecimento/modelagem/`)
